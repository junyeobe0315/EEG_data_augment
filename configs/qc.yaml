psd:
  enabled: true
  band: [8.0, 30.0]
  z_threshold: 2.5
covariance:
  enabled: true
  dist_threshold_quantile: 0.95

# If QC is too strict, optionally relax to keep at least this ratio.
# Set to 0.0 to disable relaxation.
target_keep_ratio: 0.0

# Oversampling control
max_attempt_factor: 3.0
